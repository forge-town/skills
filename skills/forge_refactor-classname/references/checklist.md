# ClassName 重构完成清单

执行完毕后逐项确认：

- [ ] 所有目标文件已递归扫描（包含 `.tsx`、`.jsx`、`.vue`）
- [ ] 全部模板字符串形式的 `className` 已转换为 `cn()` 调用
- [ ] 所有已转换文件顶部已添加 `import { cn } from "@/lib/utils"`（如未已导入）
- [ ] 转换报告包含：文件列表、className 位置、转换前后对比
- [ ] 转换后类名逻辑等价（动态条件类名行为未改变）
