# 组件预览生成完成清单

执行完毕后逐项确认：

- [ ] 已生成 `{ComponentName}.temp.json` 结构化描述文件
- [ ] 已生成 `{ComponentName}.temp.md` 目录树可视化文件
- [ ] JSON 包含：子组件列表、props 结构、状态信息、hooks 调用、复杂度评估
- [ ] Markdown 包含：组件关系可视化、复杂度指标、初步拆分建议
- [ ] 文件命名使用 `.temp.` 中缀（放置于组件所在目录）
